# 腾讯云发布记录

## 2026-09-16b（小修复：自定义字库"每局抽取"文案数字对不上）

- 服务：`hanzi-game`
- 对外端口：`8082`（容器端口 `3000`）
- 发布镜像：`hanzi-game:release-20260916-1142`
- 镜像摘要：`sha256:46f7b3841e39aafb4e35028fbb20ac5a446f27fde0797e5ce5c792074de66883`
- **回退镜像：`hanzi-game:release-20260916-0939`**（上一版，仍保留在服务器上）
- 发布包 SHA-256：`15915f13f3341d7f967440397ec4b9d5754e883ee68c6d36ee75fad72755570b`
- 提交：`03f8bdb`
- 运行策略：`unless-stopped`
- 数据卷不变：`/opt/hanzi-game-images` → `/data/word-images`（此次未动，部署后确认图库文件数量与部署前一致）

### 本次内容

自定义字库预览弹窗写死"每局随机抽取：18 个"，但棋盘固定 4×4=16 格，
实际每局只抽 8 对。改成共享常量 `CUSTOM_LEVEL_BOARD_ROWS/COLS/PAIR_COUNT`
（types.ts），GameScreen 铺棋盘和 CustomTab 显示文案统一取值。

### 验证

- `npx tsc --noEmit` 零错误
- 本地 Playwright 实测：粘贴 20 词保存，预览弹窗正确显示"每局随机抽取：8 个"
- 公网 10 项验证全通过（含生图强制重生 11s、图库命中 0s）
- 部署前后图库文件数量一致（4 张），确认未受影响

---

## 2026-09-16（生图成本优化：固定狐狸 + 场景随词 + 服务器共享图库）

- 服务：`hanzi-game`
- 对外端口：`8082`（容器端口 `3000`）
- 发布镜像：`hanzi-game:release-20260916-0939`
- 镜像摘要：`sha256:a108eeffd7e52c691101d39e402dfe289353300ab60ee55a40052cee235afb4b`
- **回退镜像：`hanzi-game:release-20260915-2202`**（上一版，仍保留在服务器上）
- 发布包 SHA-256：`ca1ffcf4832dbdb9ccd5dd851447515e3c30b6e2dbac07332a026998674bf481`
- 提交：`707aca2`
- 运行策略：`unless-stopped`
- **新增数据卷**：`/opt/hanzi-game-images` → 容器内 `/data/word-images`
  （词卡图库落盘目录，之前没有任何数据卷；这次起必须保留，删除前确认没有需要保留的图）

### 本次内容

1. **角色收敛**：8 选 1 动物 → 固定狐狸，向导三步瘦成一步（只起名）；
   旧档案（非狐狸）会被强制迁移并重新弹向导
2. **生图不再传场景**：改成固定狐狸 + 「背景贴合词意」，AI 自己判断场景，
   避免"大海配太空"式矛盾画面，也让同一个词的提示词完全确定
3. **服务器共享图库**：新增 `lib/imageLibrary.ts` + `/api/word-image/[word]`，
   `/api/generate-image` 改成查库优先。图片压缩为 768px WebP，
   2.2MB PNG → 约 80-100KB，缩小 20+ 倍
4. **修复三处既有缺陷**：「换一张图」此前三重锁死现已可用；去重键统一为
   `word`（此前读写不一致导致跨关卡命中会产生重复记录）；删除未被调用的
   生图队列死代码

### 发布前验证

- `npx tsc --noEmit` 零错误
- **本地容器实测 sharp**（跨架构构建的已知风险点）：Next.js standalone 自动
  正确追踪 sharp 原生库，无需额外配置；实测压缩、落盘、并发去重、路径穿越
  防护、"换一张图"完整 UI 流程（RewardCardModal + WordBookScreen 两处入口）
  全部通过

### 发布后验证（公网 10 项全通过，含新增的图库专项）

| 项目 | 结果 |
|---|---|
| 首页 / 字体 ×2 / levels.json | HTTP 200，无谷歌字体外链 |
| 生故事（DeepSeek） | 真实生成，无 fallback 标记 |
| 朗读（微软 Edge TTS） | 正常返回音频 |
| 生图强制重生 | 12 秒，成功落盘 |
| 图库文件下载 | HTTP 200 |
| **图库命中（不带 force）** | **0 秒，URL 不变，未触发生图 API** |

**数据卷持久化专项**：重启容器前后对比 `/opt/hanzi-game-images` 目录，
`生日.webp` 文件完全一致，重启后仍可通过 `/api/word-image/生日` 正常访问——
证实图库不会因容器重建/重启丢失。其余 7 个容器全程未受影响，磁盘 85%。

---

## 2026-09-15（双栏连线玩法 + 二年级上课程）

- 服务：`hanzi-game`
- 对外端口：`8082`（容器端口 `3000`）
- 发布镜像：`hanzi-game:release-20260915-2202`
- 镜像摘要：`sha256:c2b1ad809e3f5c14b051cd9812ff1073e2e08158f3445609b2221dc13517349e`
- **回退镜像：`hanzi-game:release-20260914-1237`**（上一版，仍保留在服务器上）
- 发布包 SHA-256：`405b18485202a8fd5308699a17ce033b5918ecf0e3360dbe05c61a884b0cb187`
- 提交：`a7c2b6e`
- 运行策略：`unless-stopped`

### 本次内容

1. **玩法改版**：网格 → 双栏（左栏词首字 / 右栏词尾字），新增 SVG 连线层
   （提示绿虚线 / 成功橙实线 / 点错红虚线），补上此前从未触发过的点错震动反馈。
2. **课程切换**：一年级下 → 二年级上（`grade2_semester1.json`，8 单元 160 词对），
   字体子集按新课程重新生成（1038 → 1164 字）。一年级数据保留，可随时回切。
3. **同步修复**：`useSaveData` 写死的首关 ID `g1s2u1` 在换课程后会导致全部关卡锁死，
   已改为常量并让「列表第一关永远可玩」按位置判定。

### 发布前验证

- `npx tsc --noEmit` 零错误；160 个词对通过格式/查重/生字覆盖校验
- 本地容器冒烟：首页、二年级课程数据、两个字体文件均 200，页面无谷歌字体外链

### 发布后验证（公网 8 项全通过）

| 项目 | 结果 |
|---|---|
| 首页 / 字体 ×2 / levels.json | HTTP 200，无谷歌字体外链 |
| 生故事（DeepSeek） | 真实生成，无 fallback 标记 |
| 朗读（微软 Edge TTS） | 正常返回音频 |
| 生图（阿里云百炼） | 18 秒出图，响应 2.9 MB |

专项验证：线上课程接口返回二年级上 8 单元；字体含全部二年级新字（蝌蚪狐狸雾葫芦等）；
**浏览器带一年级旧存档访问，首关仍可正常开始**（此为换课程最易翻车的回归点）。
其余 7 个容器全程未受影响。

---

## 2026-09-14（首次上线）

- 服务：`hanzi-game`
- 对外端口：`8082`（容器端口 `3000`）
- 当前镜像：`hanzi-game:local`
- 发布镜像：`hanzi-game:release-20260914-1237`
- 镜像摘要：`sha256:34982c4fc5539d38742051edbf30816f366ac5a56e888785eb942fef47a3d4ce`
- 回退镜像：无（首次发布，无上一版本）
- 发布包 SHA-256：`86f0576f8cfb97de40229a8b15e2d2a96efd7ddcd591c9b1627d0f8428b03e81`
- 镜像架构：`linux/amd64`（本机为 Apple Silicon，必须 `docker buildx build --platform linux/amd64`）
- 运行策略：`unless-stopped`
- 密钥文件：`/root/hanzi-game.env`（权限 600，含 DASHSCOPE_API_KEY / DEEPSEEK_API_KEY，不进镜像）

### 本次随部署一并完成的改动

1. **字体自托管**：原先从 `fonts.googleapis.com` 加载 Ma Shan Zheng / ZCOOL KuaiLe，国内访问会超时导致字体退化。
   现裁剪为仅含界面用到的 1038 个汉字并放入 `public/fonts/`（7 MB → 554 KB）。
   字表由 `scripts/build-font-subset.mjs` 生成，新增关卡词后重跑该脚本再执行 pyftsubset 即可。
2. **修复故事生成静默降级**：`deepseek-flash` 是推理模型，推理会吃掉 `max_tokens` 预算
   （实测 1600–8000 token），超出后 `content` 返回空，接口静默返回本地模板故事且 HTTP 仍为 200。
   实测失败率约 50%。改为 `thinking: { type: 'disabled' }`：耗时 31s → 2-3s，输出 token 降约 35 倍，
   连续 5 次全部真实生成。见 `lib/gemini.ts`。

### 发布前验证

- 本地生产构建通过，standalone 产物正常
- 本地容器冒烟：首页 200、字体 200、故事连续 5 次真实生成

### 发布后验证（经 SSH 隧道，8 项全通过）

| 项目 | 结果 |
|---|---|
| 首页 | HTTP 200 |
| 本地字体 ×2 | HTTP 200，页面无谷歌字体外链 |
| levels.json | HTTP 200 |
| 生故事（DeepSeek） | 真实生成，无 fallback 标记 |
| 朗读（微软 Edge TTS） | 正常返回音频（依赖 Dockerfile 里的 `--dns-result-order=ipv4first`） |
| 生图（阿里云百炼） | 19 秒出图，响应 3.1 MB |

容器日志无 error；内存占用 108.7 MiB。

### 部署时顺带处理

服务器磁盘原为 95%（仅剩 2.3 GB），清理 19 个悬空镜像 + 未使用构建缓存后降至 83%（可用 7.0 GB）。
所有带标签镜像与 7 个运行中容器均未受影响。

### 公网访问

- 访问地址：http://211.159.160.4:8082
- 腾讯云安全组已放行 `8082`（入站 TCP，0.0.0.0/0），与 8080/8081 并列。
- 公网端到端验证：8 项全部通过（含生图 20s / 3.2 MB 响应）。

服务器连接参数保存在本地 `.env.deploy`，不写入发布记录。
